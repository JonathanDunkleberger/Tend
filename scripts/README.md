# scripts/

Local helpers for marketing screenshots and static HTML captures used in the README.

```bash
# Example: render a phone-width shot (see render-shot.mjs / preview-shot.mjs)
node scripts/render-shot.mjs
```

Committed images live in `scripts/shots/` and are referenced from the root README.